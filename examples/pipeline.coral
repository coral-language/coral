fn main() do
  numbers = [1, 2, 3, 4, 5]
  
  result = numbers
    .filter(isEven)
    .map(double)
    .sum()
  
  print(result)
  return 0
end

fn isEven(x: Int) -> Bool do
  return x % 2 == 0
end

fn double(x: Int) -> Int do
  return x * 2
end